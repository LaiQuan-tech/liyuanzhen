/**
 * 語音辨識的專有名詞提示。
 *
 * ⚠️ 這不是裝飾用的，是準確度的關鍵。實測（2026-08-19，`npm run verify:stt`）：
 *
 *   沒有這份清單：「現在行政院的**新平會**有40位幕僚」  ← 錯
 *   有這份清單：  「現在行政院的**性平會**有40位幕僚」  ← 對
 *
 * 逐字稿錯了，後面整條鏈路都會錯：embedding 算出來的向量不同 → 檢索不到正確的
 * 語料段落 → 她答非所問，或直接說「這超出我手上資料的範圍」。
 * 訪客看到的是她聽不懂人話，而不是「辨識有點小誤差」。
 *
 * 維護原則：**只放語料裡真的談到、而且讀音容易被聽錯的詞**。
 * 清單變長不會提高準確度，只會讓模型更容易把不相干的詞硬套上去。
 * 語料改版時（content/knowledge/）順手回來看一眼。
 */
export const STT_VOCABULARY = [
  // 人與組織
  "李元貞",
  "婦女新知",
  "婦女新知基金會",
  "婦權會",
  "性平會",
  "晚晴協會",
  "勵馨",
  // 機關
  "行政院",
  "立法院",
  "教育部",
  // 法案與著作
  "民法親屬編",
  "眾女成城",
  // 領域用語
  "婦運",
  "婦女運動",
  "性別平等",
  "女性主義",
  "父權",
  // 地名（歷史事件相關）
  "華西街",
] as const;

/** 組進轉錄提示裡的那一段。獨立出來是為了讓 lib/stt 與驗證腳本吃同一份。 */
export const STT_VOCABULARY_HINT = `

這段話的主題是台灣婦女運動與李元貞。可能出現的專有名詞（聽到相近讀音時優先採用）：
${STT_VOCABULARY.join("、")}。`;
