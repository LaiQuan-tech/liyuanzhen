/**
 * 全站文案與設定的單一來源。改文字改這裡，不要進 JSX。
 */

export const site = {
  name: "李元貞 × AI 數位人",
  title: "李元貞 × AI 數位人｜線上就能親口問她",
  description:
    "台灣婦運先驅李元貞的 AI 數位人。問婦女運動的歷史，問她的生平與著作，她會用自己的聲音回答。內容由 AI 依她的著作與公開資料生成。",
  /** 網站所有權。⚠️ 不是製作單位——製作單位不出現在站上。 */
  owner: "財團法人婦女權益促進發展基金會",
} as const;

/** 數位人的顯示名稱——刻意永遠帶著「AI 模擬」，不做成可關閉的橫幅 */
export const AVATAR_NAME = "數位李元貞（AI 模擬）";

/** 每一則 AI 回答下方都必須出現這句，不可省略 */
export const ANSWER_DISCLAIMER =
  "本回答由 AI 依公開資料生成，非李元貞老師本人發言。";

/**
 * 每一頁頁尾都必須出現的揭露。不做成可關閉的橫幅。
 *
 * ⚠️ 這段話刻意從 `DEMO_NOTICE` 改名而來——改名不是為了好聽，是為了讓所有
 * 引用點在**編譯期**就報錯。舊名的內容是「尚未取得肖像與內容授權」，
 * 授權簽妥之後那句話變成假話，而漏改一處就是站上留著一句謊。
 * 靠改值不改名的話，漏了也沒人會發現。
 *
 * ⚠️ 它同時被鏡射進 RAG 語料（content/knowledge/07-about-this-site.md）。
 * 改這裡就要一起改那邊，然後跑 `npm run build:index` 與 `npm run ingest:supabase`，
 * 否則訪客問到同一題時，AI 會照舊索引回答，當場否定網站自己。
 */
export const SITE_NOTICE =
  "「數位李元貞」是 AI 分身，不是李元貞老師本人。肖像與聲音經老師書面授權使用，回答依其著作與公開資料生成，不代表她的立場。";

/** 超出知識庫範圍時的婉拒文案（不呼叫 LLM，直接回這段） */
export const OUT_OF_SCOPE_REPLY =
  "這個問題超出我手上資料的範圍，我不便隨口回答。我能談的是李元貞老師的生平、婦女新知的創辦過程，以及台灣婦女運動的重要歷程。";

/** 觸發封鎖清單時的替代回答 */
export const GUARDED_REPLY =
  "這部分我不便代為表態。我能談的是李元貞老師的婦運歷程與著作，要不要換個方向問問看？";

/**
 * 只列已完成的頁面——導覽列連到 404 在客戶面前是最沒必要的失分。
 * 時間軸／金句／提問牆／關於李元貞完成後再加回來。
 */
export const nav = [
  { href: "/live", label: "面對面問她" },
  /**
   * ⚠️ 這一項跟上一項是**同一個功能的兩個版本**（坐姿／站姿），不是兩個功能。
   * 所以標籤刻意寫成「（站姿）」而不是另取一個名字——訪客要看得出來
   * 點哪個都會走到同一件事，只是人長得不一樣。
   * 兩頁各自會開一個計費中的 session，同時開著就是兩份錢。
   */
  { href: "/live2", label: "面對面問她（站姿）" },
  { href: "/chat", label: "文字對話" },
  { href: "/events", label: "活動報名" },
  { href: "/about-ai", label: "關於這個 AI" },
] as const;

/**
 * 只放 nav 沒有的。/about-ai 已經在 nav 裡，兩邊都列會讓頁尾出現兩次同一個連結。
 */
export const footerLinks = [{ href: "/privacy", label: "隱私權" }] as const;

/**
 * /live（全螢幕語音互動）的文案。
 *
 * 🔴 2026-09-03 互動從「按住說話」改成「點一下開始、再點一下結束」，
 * 這一整組文案跟著重寫。改動時整組要一起看——半組是按住式的講法
 * 會直接把人教錯（他會按住不放，然後永遠等不到送出）。
 *
 * 沿用 Sunny 展場版已經在現場驗過的那一套講法——那些字是對著真的訪客
 * 用過的，比重新想一套安全。
 *
 * ⚠️ 錯誤文案一律**不會**被唸出來，只顯示在字幕上。
 * 理由：/api/chat 回 403 / 429 / 400 時 body 是錯誤訊息不是答案，
 * 少了這道分界，她會用克隆的聲音唸出「請求格式錯誤。」
 */
export const liveCopy = {
  /** 還沒開始，等訪客第一次按下去 */
  ready: "點一下下面的按鈕開始說，說完再點一下",
  talkIdle: "開始說話",
  talkRecording: "說完了，送出",
  /**
   * 錄音中的說明。
   *
   * ⚠️ 只講事實，**不要**寫成判斷句（「講大聲一點」那種）。
   * 實測安靜房間的底噪就有 0.007、而說話的瞬時值在字與字之間會掉回底噪，
   * 任何絕對門檻都會在使用者正常講話時反覆喊「沒收到」——冤枉人又嚇人。
   * 那一版真的上線過，使用者回報「按了沒反應」有一半是被這句話誤導的。
   * 要證明「聲音有進來」，靠會跟著音量跳的音量計，不要靠文字。
   */
  recordingHint: "錄音中，說完點一下按鈕",
  /**
   * 快撞到自動停止時才換這句，並且改成倒數。無預警被切斷比較難理解。
   *
   * ⚠️ 這句在切換式下比按住式重要得多：按住式有人的手當上限，手一放就結束；
   * 切換式允許「按了就走開」，那個計時器是唯一的收手機制，所以要先講。
   * ⚠️ 秒數由畫面即時算，不要寫死在文案裡——上限改了文案就會說謊。
   */
  recordingNearCap: "時間快到了，說完點一下按鈕",
  connecting: "正在接通…",
  transcribing: "聽你說…",
  thinking: "正在查資料…",
  speaking: "回答中",

  micDenied: "沒有麥克風權限，無法用語音提問。你可以改用文字版。",
  micUnavailable: "找不到可用的麥克風。你可以改用文字版。",
  micUnsupported: "這個瀏覽器不支援語音輸入，建議用 Chrome，或改用文字版。",
  offline: "網路暫時不穩，請稍候再試一次。",
  notHeard: "抱歉，我沒有聽清楚，請再試一次。",
  /**
   * ⚠️ 這三句是後來補的，因為原本「空的逐字稿就安靜地忽略」——
   * 使用者按住、講話、放開，然後畫面什麼都沒發生。那就是「沒反應」，
   * 是這個介面最糟的失敗方式：他不知道是自己的問題還是網站壞了。
   */
  tooShort: "沒收到聲音，再點一次試試。",
  /**
   * 第一次來訪必踩的洞，不是誤觸。
   *
   * ⚠️ 這句原本是為「按住說話」寫的：按住 → 跳權限詢問 → 想按「允許」就
   * 必須先放開按鈕 → pointerup → 判定 aborted，於是每個新訪客的第一次
   * 都注定失敗。**切換式已經沒有那條路徑了**（按「允許」不會停止錄音）。
   *
   * 但這句話還是要留：`RecordingOutcome.aborted` 仍然到達得到——
   * `start()` 還沒 resolve 就再點一下的時候（lib/live/recorder.ts 的
   * `abortPending`）。那種情況下權限詢問也可能還開著，這句話仍然是對的。
   *
   * 用 navigator.permissions 確認權限還停在 prompt 才顯示這句，
   * 真正的誤觸仍然走 tooShort。
   */
  micNeedsAllow: "請先在瀏覽器的提示裡按「允許」使用麥克風，然後再點一次開始說話。",
  noSound: "沒有收到聲音。請確認麥克風有開，或講大聲一點。",
  /**
   * 錄音管線死掉——按住夠久卻一塊音訊都沒收到。
   *
   * ⚠️ 這句**不可以**跟 tooShort 合併。使用者確實按住了、也講了話，
   * 叫他「按住不放，講完再放開」是指著錯的方向，他會一直重試同一個動作。
   * 真實案例：睡眠喚醒之後 AudioContext 停止 render，連按十次都是這個結果。
   */
  micDead: "麥克風沒有送出任何聲音。請重新整理這一頁；如果還是一樣，看看是不是有別的分頁或程式正在使用麥克風。",
  heardNothing: "沒有聽出內容，再說一次看看。",
  failed: "抱歉，我需要休息一下，請稍候再試一次。",
  /**
   * ⚠️ 影像還在、只有聲音失敗時用這句，**不要**跟 failed 合併。
   * 兩者對訪客的意思完全不同：failed 是「這一輪沒有答案」，
   * 這一句是「答案在上面，只是這次沒唸出來」。
   * 少了它，TTS 失敗在畫面上就跟當機一模一樣。
   */
  voiceFailed: "這次的聲音沒出來，答案在上面。再問一次通常就正常了。",

  /**
   * session 到期。⚠️ 這不是錯誤，是預期中的行為——
   * 伺服器的 max_session_duration 到了就會收掉。要講得像正常流程，
   * 不要讓訪客以為壞了。
   */
  sessionEnded: "這段連線結束了。點一下按鈕就可以重新開始。",

  /** 回文字版的出口。麥克風不能用時這是唯一的路。 */
  fallbackLink: "改用文字版",
} as const;
