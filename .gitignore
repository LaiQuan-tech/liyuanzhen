# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo

# 本機預覽用的參考圖。刻意不進 repo：
# 這些是他人拍攝的既有照片，著作權不在我們手上（見 docs/AVATAR-RUNBOOK.md 附錄 B），
# 而且站上的護欄文案目前還寫著「尚未取得肖像授權」。commit 進去就會跟著部署上線。
public/dev/
