import type { Metadata } from "next";
import LiveStage from "@/components/live/LiveStage";
import { POSE_STANDING_STAGE } from "@/components/avatar/full-body-stage";

export const metadata: Metadata = {
  title: "虛擬互動（滿版）｜李元貞 × AI 數位人",
};

/**
 * `/live` 的第三個版本：人物跟 `/live2` 一模一樣（站著、捧著一本翻開的書），
 * 差別在**背景是滿版的**——`/live` 與 `/live2` 在寬螢幕上左右各露出一大條
 * `#1A1A1A`（2000px 寬的視窗只有中間約 760px 是內容，其餘 62% 是黑的）。
 *
 * 頁面本身跟 `/live`、`/live2` 一樣是一層薄殼，同樣**刻意不掛** `<Nav />` 與
 * `<Footer />`——這一頁的重點就是那個佔滿螢幕的人，上下各一條列會把它切碎。
 * 代價是頁首頁尾常駐的揭露不會跟過來，所以 LiveStage 自己把 AVATAR_NAME、
 * ANSWER_DISCLAIMER、SITE_NOTICE 與影片上的常駐浮水印全部放回畫面上。
 * 那不是可選的裝飾，見該元件的檔頭。
 *
 * ⚠️ 它跟 `/live2` **共用同一組人物幾何**（`POSE_STANDING_STAGE.box` 直接引用
 * `POSE_STANDING.box`）。要調對位就是兩頁一起調，不要只改一邊。
 *
 * 🔴 三頁各自會開一個**計費中**的 LiveAvatar session。同時開著就是三份錢。
 *
 * ---
 *
 * ## 🔴 這裡曾經是一張真實街景，為什麼換掉
 *
 * 第一版的背景是從她受訪照片衍生出來的街景板（木門＋藍柱＋人行道）。
 * 使用者連續四次回報「怪怪的」，我修了四輪才量出根因，過程值得留著：
 *
 * | 試過 | 結果 |
 * |---|---|
 * | 調 α 帶寬與曲線（掃 2/4/7/14px、γ 0.45–1.0） | 白鑲邊與被吃掉互相打架，到頂 |
 * | 亮度夾 ＋ 形態學開運算 | 修掉白刺與白斑，有效但不夠 |
 * | 分層景深 ＋ 提亮背景 | 地面出來了，明顯進步 |
 * | Light wrap（環境光包覆） | **更糟**，變成外發光 |
 * | 調亮她的下半身 | **更糟**，黑外套變灰 |
 *
 * 真正的原因量出來是這樣（亮度，由上到下）：
 *
 * ```
 * 她的下半身    47 → 47 → 50 → 51 → 49     極差  4
 * 她站的場景   116 → 123 → 111 → 109 → 111  極差 14
 * 橫掃她的外套  48 · 47 · 64 · 45 · 49      極差 19
 * ```
 *
 * 底圖胸口以下是 AI 生成的**平光**——沒有任何方向性明暗；而真實街景是**有
 * 方向的自然光**。這個不match在人物的**內部**，不在邊緣，所以去背怎麼修都到不了。
 *
 * ⚠️ **不要再把她放進任何「宣稱是真實地點」的背景**，除非底圖重新生成。
 * 重生成的代價不小：整組臉部對位要重量（髮頂→脖子、影片框四個百分比、
 * 橢圓烘焙 1.02→1.12、gamma 1.175），而 poses.ts 記著站姿那張當初量錯過一次、
 * 更早一版「手指是斷的」。
 *
 * ## 現在的背景板 `public/live-bg-stage.webp` 是什麼
 *
 * ⚠️ 2026-09-08 又換了一次：使用者要求「背景用跟首頁上照片類似的背景」。
 * 現在是從**首頁那張照片**（`public/liyuanzhen.jpg`，已水平翻轉，木門在左、
 * 藍柵欄與她在右）裁出 `x 0–612` 的乾淨門區，左右鏡射拼成 16:9，
 * 分層失焦（遠處高斯 11、近地面 2.5）、亮度 ×1.10、上下烘漸層。
 *
 * ⚠️ 裁切收在 x=612 是量出來的：`x 600–649` 的底部已經有 33% 暗像素、
 * `x 650+` 是 100%——那是她的腿。第一版裁到 650，鏡射之後在畫面底部中央
 * 長出一根黑色尖刺。
 *
 * 🔴 **這是知情的取捨，代價已經量過。** 去背檔的殘留邊緣是中性淺灰，
 * 在暖色背景上不只亮度不同、色相也不同，所以更明顯：
 * 邊緣過亮的像素從中性板的 1386 個變成 5718 個。
 * 要換回最乾淨的版本，把 `live-bg-stage.webp` 換成中性板即可，程式碼不用動。
 *
 * ---
 *
 * ## 上一版：程式生成的抽象背景板（保留說明，換回去時要用）
 *
 * 抽象背景板（2560×1440，53KB），刻意不假裝是任何地方：
 * 中性色的漸層＋主體後方的柔光暈＋幾顆散景光點＋四角壓暗＋底部收暗。
 * 腦袋一旦不把它當照片看，就不會去比對光線對不對，平光的人物放上去反而成立。
 *
 * 🔴 **選中性淺色不是美感偏好，是量出來的。** 底圖的邊緣是對著淺灰
 * `rgb(233,237,236)` 算的，殘留誤差正比於新背景與它的亮度差。
 * 同一份去背檔換三種背景，邊緣過亮的像素數：
 *
 * ```
 * 街景板（平均亮度 93）      8138
 * 品牌紫（91）              3731
 * 暖色場（86）              4606
 * 中性淺色（119.5）         1386   ← 少 83%
 * ```
 *
 * ⚠️ 大面積漸層一定要加雜訊（這裡是 ±3.2 階），否則 WebP 會出現色帶。
 *
 * ⚠️ 底部收暗是**做在圖檔上、不是調 CSS**。`LiveStage` 底部那條
 * `from-ink via-ink/85 to-transparent` 是字幕與法定揭露可讀性的唯一保障，
 * 不能為了多露一點背景而調淡。現在字幕帶最亮 180，疊上那條 85% ink 之後
 * 白字對比 16.5:1（AAA 是 7:1）。
 *
 * ✅ 附帶好處：抽象背景讓「這是合成影像」這件事更明顯，反而降低了
 * 把整張畫面誤讀成真實照片的風險——那正是 AvatarStage 浮水印註解在擔心的事。
 *
 * 人物去背的作法在 `components/avatar/poses.ts` 的 `POSE_STANDING_STAGE`。
 */
export default function LiveFullBleedPage() {
  return <LiveStage pose={POSE_STANDING_STAGE} />;
}
